# [Day 10] Next.js 최적화 및 렌더링 아키텍처 브리핑: 성능과 비용을 잡는 핵심 전략

Next.js 프레임워크의 핵심 기술인 이미지 최적화, 현대적 렌더링 전략(PPR 등), App Router 마이그레이션 사례 및 Vercel 운영 비용 최적화 방안을 시니어 관점으로 종합 분석합니다.

---

## 1. 핵심 요약 (Executive Summary)

본 문서는 Next.js 기반 애플리케이션의 성능 극대화와 인프라 비용 절감을 위한 핵심 아키텍처 전략을 다룹니다. 주요 결론은 다음과 같습니다.

- **성능의 핵심, 이미지 최적화**: 웹 페이지 무게의 상당 부분을 차지하는 이미지는 LCP(Largest Contentful Paint)에 직결됩니다. `next/image`를 통한 자동 크기 조정 및 시각적 안정성 확보가 필수적입니다.
- **부분 사전 렌더링(PPR)의 도입**: 정적 페이지의 속도와 동적 데이터의 신선함을 동시에 제공하는 PPR은 LCP를 10배 이상 개선할 수 있는 혁신적인 아키텍처입니다.
- **App Router로의 패러다임 전환**: Pages Router에서 App Router로의 전환은 단순한 경로 변경을 넘어, 서버 컴포넌트(RSC)와 서버 액션(Server Actions)을 활용한 아키텍처 현대화를 의미합니다.
- **전략적 비용 관리**: Vercel 운영 비용은 대역폭과 서버리스 함수 실행 시간에 좌우됩니다. 렌더링 전략 최적화와 Fluid Compute 활용을 통해 운영 비용을 40~80%까지 절감할 수 있습니다.

---

## 2. 이미지 최적화 (Image Optimization)

웹사이트 페이지 무게의 큰 부분을 차지하는 이미지는 성능 지표인 LCP에 지대한 영향을 미칩니다. Next.js는 `next/image` 컴포넌트를 통해 HTML `<img>` 요소를 확장한 강력한 자동 최적화 기능을 제공합니다.

### 2.1 핵심 기능

- **크기 최적화**: 각 장치에 적합한 크기의 이미지를 제공하며 WebP, AVIF 등 최신 형식을 지원합니다.
- **시각적 안정성**: 이미지가 로드될 때 발생하는 레이아웃 이동(CLS, Cumulative Layout Shift)을 자동으로 방지합니다.
- **빠른 로딩**: 뷰포트에 들어올 때만 이미지를 로드하는 네이티브 지연 로딩(Lazy Loading)과 블러 업(Blur-up) 플레이스홀더를 지원합니다.
- **자산 유연성**: 원격 서버의 이미지도 온디맨드로 크기를 조정할 수 있습니다.

### 2.2 구현 및 Sizing 전략

이미지 기반 레이아웃 이동을 방지하기 위해 다음 세 가지 방법 중 하나로 크기를 조정해야 합니다.

1. **정적 Import**: 로컬 이미지를 `import` 하여 본질적인 크기를 자동으로 결정.
2. **수동 지정**: `width`와 `height` 속성을 명시하여 가로세로 비율 유지.
3. **Fill 방식**: 부모 요소를 채우도록 설정(`fill` 속성). 이때 부모 요소는 `position: relative` 및 `display: block` 상태여야 합니다.

---

## 3. 현대적 렌더링 전략 분석

Next.js는 콘텐츠의 성격에 따라 다양한 렌더링 전략을 선택할 수 있는 유연성을 제공합니다.

### 3.1 렌더링 전략 비교

- **CSR (Client-Side Rendering)**: FCP 지연으로 속도는 다소 느리나 실시간 렌더링 가능 (대시보드, 인증 후 전용 페이지)
- **SSR (Server-Side Rendering)**: 서버 연산 대기로 속도는 보통이나 항상 최신 데이터 유지 (이커머스, 뉴스, 개인화 콘텐츠)
- **SSG (Static Site Generation)**: CDN 캐싱을 통해 매우 빠름, 단 빌드 시점에 고정됨 (블로그, 문서, 마케팅 페이지)
- **ISR (Incremental Static Regeneration)**: SSG의 장점에 주기적 갱신을 더해 빠름 (대규모 카탈로그, 빈번한 업데이트 블로그)
- **PPR (Partial Prerendering)**: 정적 쉘과 동적 스트리밍의 하이브리드 결합으로 초고속(<100ms) LCP 제공

### 3.2 부분 사전 렌더링 (Partial Prerendering, PPR)

PPR은 정적(SSG)과 동적(SSR) 렌더링 사이의 선택 고민을 해결하는 게임 체인저입니다.

- **작동 원리**: 빌드 시점에 정적 HTML 쉘을 생성하고, `<Suspense>` 경계 내부의 동적 컴포넌트는 요청 시점에 스트리밍으로 전달합니다.
- **성능 이점**: 정적 쉘이 CDN 엣지에서 즉시 서빙되므로 TTFB와 LCP가 비약적으로 향상됩니다 (사례 분석 결과 LCP 680ms → 65ms 개선).
- **Next.js 16 업데이트**: 실험적 플래그가 제거되고 `cacheComponents` 구성을 통해 안정적인 기능을 제공합니다.

---

## 4. App Router 마이그레이션 및 RSC 아키텍처

React Server Components(RSC)를 기반으로 하는 App Router는 현대적 웹 애플리케이션의 새로운 표준입니다.

### 4.1 마이그레이션의 도전 과제와 해결 (Money Forward 사례)

- **페이지 확장자 문제**: App Router에서 `pageExtensions` 매개변수 지원 미비 시, 기존 Pages Router 페이지를 프록시 처리하는 스크립트로 이중 실행(Dual-running) 환경을 구축하여 해결.
- **런타임 구성(Run-time Config)**: 빌드 시 정적 분석을 중시하는 App Router 특성상 기존 `publicRuntimeConfig`가 작동하지 않으므로, `next-runtime-env` 등의 추상화 레이어가 도입되어야 합니다.
- **에러 처리 및 인증**: 서버 측 "Not Found" 처리와 클라이언트 측 Error Boundary 분리 설계, 미들웨어를 통한 중앙 집중식 인증 관리가 권장됩니다.

### 4.2 서버 컴포넌트(RSC)의 가치

- **번들 크기 감소**: 서버에서만 실행되는 RSC는 클라이언트로 불필요한 JavaScript를 전송하지 않습니다.
- **데이터 페칭 최적화**: 서버 액션(Server Actions)을 통해 폼 변이(Mutation)를 처리하고 복잡한 비즈니스 로직을 서버 단으로 통합하여, 보안과 성능을 한 번에 확보합니다.

---

## 5. Vercel 운영 비용 최적화 (Cost Optimization)

Vercel 비용은 트래픽 증가와 함께 급격히 상승할 수 있으며, 이 중 '대역폭(Bandwidth)'과 '서버리스 함수 실행 시간'이 비용의 핵심 요소입니다.

### 5.1 비용 절감을 위한 핵심 전략

- **렌더링 전략 최적화**: 고비용의 SSR 페이지를 정적 생성(SSG)이나 ISR로 전환 시 함수 실행 비용을 80~95% 절감할 수 있습니다.
- **서버리스 함수 효율화**:
  - **메모리 할당 조정**: 기본 1.7GB에서 256MB~512MB 튜닝.
  - **병렬 데이터 페칭**: `Promise.all` 사용하여 실행 시간 50~70% 단축.
  - **라이브러리 경량화**: Moment.js(329KB)를 date-fns(86KB)로 교체하는 등 번들을 최적화합니다.
- **Fluid Compute 도입**: 서버리스 인스턴스를 지능적으로 재사용하여 함수 실행 오버헤드를 줄이고 비용을 추가 20~30% 절감합니다.

### 5.2 대역폭 최적화

- **이미지 변환 모델 대응**: Vercel 과금 구조 상 고유한 변환(너비/높이/형식/품질 조합)당 비용이 발생하므로, 이미지 크기를 표준화하여 불필요한 재생성을 막아야 합니다.
- **Prefetch 제어**: `<Link>` 컴포넌트의 `prefetch={false}` 속성을 활용하여 중요도가 낮은 페이지의 선행 로딩으로 인한 데이터 및 대역폭 낭비를 차단합니다.

---

## 6. 결론 및 향후 방향

Next.js 아키텍처는 서버와 클라이언트의 경계를 재정의하고 있습니다. PPR과 RSC의 결합은 사용자 경험(렌더링 속도)과 개발 효율성(서버 로직 통합)을 동시에 만족시킵니다.

효과적인 애플리케이션 운영을 위해서는 **정적/동적 경계**를 아키텍처 단계부터 명확히 하고, 주기적인 비용 분석과 인프라 최적화(Fluid Compute 활성화, 메모리 파인 튜닝, 대역폭 방어 등)를 병행하는 전략적 접근이 필수적입니다.
